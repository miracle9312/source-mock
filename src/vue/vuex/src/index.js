import { Store, install } from "./store";
import { mapActions, mapGetters, mapMutations, mapState } from "./helpers";

export default {
  install,
  Store,
  mapActions,
  mapGetters,
  mapMutations,
  mapState
};
